package csv.converter.sfedu;

import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;

public class CSVReader {

    public String[] convert() {
/*
        String csvFile = "/home/dann/Customer_1.csv";
        String line = "";
        String cvsSplitBy = ",";

        try (BufferedReader reader = new BufferedReader(new FileReader(csvFile))) {

            while ((line = reader.readLine()) != null) {
                String[] customer = line.split(cvsSplitBy);
                Customer_1 cust = new Customer_1();
                cust.setName(customer[0]);
                cust.setTraffic(Double.parseDouble(customer[1]));
                cust.setDate();

            }

        } catch (IOException e) {
            e.printStackTrace();
        }
        */

        return new String[0];
    }

}
